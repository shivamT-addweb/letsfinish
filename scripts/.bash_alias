alias dup="docker-compose up -d"
alias dstop="docker-compose stop"
alias ddown="docker-compose down"

alias dcbash="docker exec -it drupal8vagrant_drupal_1 bash"
alias dbbash="docker exec -it drupal8vagrant_db_1 bash"

